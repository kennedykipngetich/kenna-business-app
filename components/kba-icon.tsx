export function KBAIcon({ className = "" }: { className?: string }) {
    return (
      <div className={`flex items-center justify-center w-8 h-8 rounded-lg bg-[#0066FF] text-white font-bold text-sm ${className}`}>
        KBA
      </div>
    )
  }
  
  